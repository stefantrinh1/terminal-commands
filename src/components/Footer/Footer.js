import React from 'react';

import { Box } from 'theme-ui';

export default function Footer() {
  return <Box></Box>;
}

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
  },
};
